import styled from "styled-components";

/**
 * SelectBox의 레이아웃을 담당하는 컨테이너
 */
const SelectBoxContainer = styled.div`
  display: flex;
  flex-direction: column;
  margin-bottom: 20px;
`;

export default SelectBoxContainer;
